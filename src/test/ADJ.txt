// ADJ
// Checks if the vertices Ans(1) and Ans(2) are adjacent
// Returns the index of the connecting edge if it exists and zero otherwise in O(E)

abs(iPart(real(Ans->thetaGT
abs(real(|LE
1+sum(not(cumSum(Ans=sum(|LthetaGT{1,|E~3}) or DAns=sum(|LthetaGT{|E~3,1
If Ans>E:0
