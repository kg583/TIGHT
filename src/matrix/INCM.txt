// INCM
// Computes the incidence matrix of the graph in [I] based on the current edge ordering
// Returns in O(E), does nothing if the graph the product VE exceeds 400

If VE≤400:Then
  {V,E→dim([I]
  For(θ,1,E
    abs(iPart(real(ʟE(θ
    D→[A](Ans,θ
    abs(ᴇ3fPart(real(ʟE(θ
    1→[A](Ans,θ
  End