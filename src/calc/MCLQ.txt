// MCLQ
// Computes the maximal clique containing the vertex Ans
// Finds an oriented clique if the graph is directed
// Returns the vertices of the clique in O(V * V)

{abs(iPart(real(Ans->thetaGT
For(Y,1,V
  For(theta,1,dim(thetaGT
    thetaGT(theta->X
    prgmthetaGTI
    If not(Ans:|E4->theta
  End
  If theta<|E4:Y->|LthetaGT(1+dim(|LthetaGT
End:|LthetaGT
