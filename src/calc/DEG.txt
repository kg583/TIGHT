// DEG
// Calculates the degree of the vertex abs(Ans)
// If the graph is directed and Ans > 0, the out-degree is calculated
// If the graph is directed and Ans < 0, the in-degree is calculated
// Returns the degree in O(E)

iPart(real(Ans→Z:0
If D>0 or Z<0:Then
  abs(Z→Z
  sum(Ans=abs(ᴇ3fPart(real(ʟE
End
If D>0 or Z>0:Ans+sum(Z=abs(iPart(real(ʟE