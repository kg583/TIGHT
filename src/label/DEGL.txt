// DEGL
// Sets the label of each vertex to its in- or out-degree based on the multiplier Ans
// If Ans < 0, the in-degree is chosen as the base label
// If Ans > 0, the out-degree is chosen as the base label
// Labels are scaled by abs(Ans)
// Returns in O(VE)

real(Ans->theta
V->dim(|LV
For(Z,1,V
  Z:prgmthetaGTDEG
  If theta<0:I
  Ansabs(theta)+[i]imag(|LV(Z->|LV(Z
End
