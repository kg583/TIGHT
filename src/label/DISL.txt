// DISL
// Labels each vertex with the shortest distance between itself and the vertex Ans
// Utilizes the Bellman-Ford algorithm, which supports negative edge weights
// Returns ~1, performing no labeling, if the graph contains a negative weight cycle and 0 otherwise in O(VE)

If E:Then
  V->dim(|LV
  prgmthetaGTDIST
  If dim(Ans:Then
    Ans+[i]imag(|LV->V:0
  Else:~1
  End
