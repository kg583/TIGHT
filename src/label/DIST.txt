// DIST
// Labels each vertex with the distance between itself and the vertex Ans
// Utilizes the Bellman-Ford algorithm
// Returns ~1 if the graph contains a negative weight cycle and 0 otherwise in O(VE)

If E:Then
  abs(real(Ans->X
  |E99:prgmthetaGTSETL
  [i]imag(|LV(X->|LV(X
  For(|LE(1),2,V
    For(theta,1,E
      |LE(theta:prgmthetaGTZ
      1:If W:imag(Z
      min(Ans+real(|LV(X)),real(|LV(Y
      Ans+[i]imag(|LV(Y->|LV(Y
    End
  End
  For(theta,1,E
    |LE(theta:prgmthetaGTZ
    ~1:If imag(Z)+real(|LV(X))<real(|LV(Y:Return
  End:0
